# Fix Bug Repo

A simple Calculator class with basic arithmetic operations.
Used as a test fixture — contains a known bug in the `divide` method.

## Running tests

```bash
python -m pytest test_calculator.py -v
```
