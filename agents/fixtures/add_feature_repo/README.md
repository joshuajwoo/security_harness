# Add Feature Repo

Math helper functions including `gcd`, `lcm`, and `fibonacci`.

The `is_prime` function is referenced in the test file but has not been
implemented yet — that's the task.

## Running tests

```bash
python -m pytest test_math_helpers.py -v
```
